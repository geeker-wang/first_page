# 🔥 纯 GitHub 解决方案 - 完整指南

## 📋 方案概述

**完全使用 GitHub 服务，无需任何服务器，零成本部署！**

```
┌─────────────────────────────────────────┐
│  GitHub Actions (定时任务)               │
│  - 每30分钟自动执行                      │
│  - 抓取知乎热榜数据                      │
│  - 保存到 data/zhihu-hot.json           │
│  - 自动提交到仓库                        │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│  GitHub 仓库 (数据存储)                  │
│  - data/zhihu-hot.json                  │
│  - 包含50条历史快照                      │
│  - 通过 GitHub API 访问                 │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│  GitHub Pages (前端)                     │
│  - Vue 3 + TypeScript                   │
│  - 直接调用 GitHub API                   │
│  - 本地缓存 + 手动刷新                   │
└─────────────────────────────────────────┘
```

---

## 🎯 核心优势

| 特性 | 说明 |
|------|------|
| 💰 **零成本** | 完全免费，无需服务器费用 |
| 🤖 **全自动化** | GitHub Actions 定时抓取 |
| 📊 **数据持久化** | 存储在 GitHub 仓库 |
| 🚀 **部署简单** | 提交代码即可运行 |
| 🔄 **手动控制** | 支持手动触发抓取 |
| 💾 **本地缓存** | 5分钟缓存，减少 API 调用 |

---

## 📁 项目结构

```
first_page/
├── .github/
│   └── workflows/
│       └── fetch-hot.yml          # 定时抓取工作流
│
├── scripts/
│   └── fetch.js                   # 抓取脚本
│
├── data/
│   └── zhihu-hot.json            # 数据文件 (自动生成)
│
├── src/
│   ├── components/
│   │   └── ZhihuHot.vue          # 热榜组件 (1029行)
│   ├── types/
│   │   └── zhihu.ts              # 类型定义
│   └── App.vue
│
├── package.json
├── vite.config.ts
└── tsconfig.json
```

---

## 🔄 数据流程

### 1. 自动抓取流程 (GitHub Actions)

```
GitHub Actions 定时器 (每30分钟)
  ↓
执行 scripts/fetch.js
  ↓
请求知乎 API (通过 CORS 代理)
  ↓
转换数据格式 (50条热榜)
  ↓
读取现有 data/zhihu-hot.json
  ↓
追加新快照 (保留最近50条)
  ↓
提交到 GitHub 仓库
  ↓
完成！
```

### 2. 前端读取流程

```
用户访问页面
  ↓
检查本地缓存 (5分钟内有效)
  ↓
缓存有效 → 使用缓存数据
  ↓
缓存过期 → 调用 GitHub API
  ↓
获取 data/zhihu-hot.json
  ↓
Base64 解码 + JSON 解析
  ↓
显示热榜数据
  ↓
更新本地缓存
```

---

## 🛠️ 核心组件详解

### 1. 抓取脚本 (scripts/fetch.js)

```javascript
// 配置
const CONFIG = {
  API_URL: 'https://api.allorigins.win/raw?url=' +
    encodeURIComponent('https://www.zhihu.com/api/v3/topstory/hot-lists/total?limit=50'),
  DATA_PATH: path.join(__dirname, '..', 'data', 'zhihu-hot.json'),
  MAX_SNAPSHOTS: 50,  // 保留50条历史
  TIMEOUT: 10000
}

// 主流程
async function main() {
  // 1. 读取现有数据
  const existingData = readData()

  // 2. 抓取新数据
  const items = await fetchZhihuHotList()

  // 3. 创建快照
  const snapshot = {
    id: `snapshot-${Date.now()}`,
    timestamp: Date.now(),
    formattedTime: new Date().toLocaleString('zh-CN'),
    count: items.length,
    items: items
  }

  // 4. 合并并保存
  const newData = [snapshot, ...existingData].slice(0, CONFIG.MAX_SNAPSHOTS)
  saveData(newData)
}
```

**关键特性**:
- ✅ 使用 CORS 代理绕过跨域限制
- ✅ 自动降级到模拟数据（首次运行）
- ✅ 保留最近50条历史记录
- ✅ 详细的日志输出

### 2. GitHub Actions (.github/workflows/fetch-hot.yml)

```yaml
name: 自动抓取知乎热榜

on:
  schedule:
    - cron: '0 */30 * * * *'  # 每30分钟
  workflow_dispatch:          # 手动触发
  push:
    branches: [main]
    paths:
      - '.github/workflows/fetch-hot.yml'
      - 'scripts/fetch.js'

jobs:
  fetch-and-save:
    runs-on: ubuntu-latest
    steps:
      # 1. 检出代码
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      # 2. 安装依赖
      - run: npm install axios

      # 3. 执行抓取
      - run: node scripts/fetch.js

      # 4. 提交数据
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/zhihu-hot.json
          git diff --quiet || git commit -m "chore: 更新知乎热榜数据"
          git push
```

**触发方式**:
- ⏰ **定时**: 每30分钟自动执行
- 🖱️ **手动**: GitHub Actions 页面点击 "Run workflow"
- 📝 **代码推送**: 修改工作流或脚本时自动执行

### 3. 前端组件 (src/components/ZhihuHot.vue)

**配置管理**:
```typescript
const config = ref<GitHubConfig>({
  username: 'geeker-wang',  // 你的 GitHub 用户名
  repo: 'first_page',       // 仓库名
  dataPath: 'data/zhihu-hot.json'  // 数据路径
})
```

**GitHub API 调用**:
```typescript
const fetchFromGitHub = async (): Promise<HotSnapshot[]> => {
  const url = `https://api.github.com/repos/${config.value.username}/${config.value.repo}/contents/${config.value.dataPath}`
  const response = await fetch(url)

  if (!response.ok) {
    throw new Error(`HTTP ${response.status}: ${response.statusText}`)
  }

  const data = await response.json()
  const content = atob(data.content)  // Base64 解码
  return JSON.parse(content)
}
```

**智能缓存**:
```typescript
// 检查缓存（5分钟内有效）
const cached = localStorage.getItem('zhihu_cache_data')
const cacheTime = localStorage.getItem('zhihu_cache_timestamp')

if (cached && cacheTime) {
  const age = Date.now() - parseInt(cacheTime)
  if (age < 5 * 60 * 1000) {  // 5分钟
    return JSON.parse(cached)  // 使用缓存
  }
}
```

---

## 🚀 快速部署 (3分钟)

### 步骤 1: 检查文件结构

确保以下文件存在：
- ✅ `.github/workflows/fetch-hot.yml`
- ✅ `scripts/fetch.js`
- ✅ `src/components/ZhihuHot.vue`
- ✅ `src/types/zhihu.ts`

### 步骤 2: 提交代码

```bash
git add .
git commit -m "feat: 纯 GitHub 方案实现"
git push origin main
```

### 步骤 3: 手动触发第一次抓取

1. 访问你的 GitHub 仓库
2. 点击 **Actions** 标签页
3. 找到 **自动抓取知乎热榜**
4. 点击 **Run workflow**
5. 等待 10-20 秒完成

### 步骤 4: 检查数据文件

1. 查看仓库中是否出现 `data/zhihu-hot.json`
2. 文件应该包含至少 1 条快照数据
3. 如果没有，检查 Actions 日志

### 步骤 5: 访问前端页面

1. 确保 GitHub Pages 已启用
2. 访问 `https://your-username.github.io/first_page/`
3. 配置你的 GitHub 信息：
   - 用户名：`your-username`
   - 仓库名：`first_page`
   - 数据路径：`data/zhihu-hot.json`
4. 点击 **保存配置**
5. 点击 **测试连接** (应该显示成功)
6. 点击 **从 GitHub 加载** (应该显示数据)

---

## 🎨 功能演示

### 配置面板
```
┌─────────────────────────────────────┐
│ GitHub 用户名: [your-username]      │
│ 仓库名:        [first_page]         │
│ 数据文件路径:  [data/zhihu-hot.json]│
│                                     │
│ [保存配置] [重置配置] [测试连接]    │
│                                     │
│ 💡 提示：GitHub Actions 每30分钟自 │
│    动抓取并保存数据                 │
└─────────────────────────────────────┘
```

### 控制面板 (配置后显示)
```
┌─────────────────────────────────────┐
│ [从 GitHub 加载] [加载最新]         │
│ [统计信息] [手动触发抓取]           │
│                                     │
│ 本地缓存: 2026-01-08 12:34:56       │
│ [清除缓存]                          │
└─────────────────────────────────────┘
```

### 数据展示
```
📊 当前热榜 (30条)  最后更新: 2026-01-08 12:34:56

1. 如何看待某科技公司发布的新产品？
   🔥 1.2M  2026-01-08 12:34:56

2. 2026年最值得期待的电影有哪些？
   🔥 850K  2026-01-08 12:34:56

...

📚 历史快照 (50条)
📅 2026-01-08 12:34:56  30条  ▼
  1. 标题1  🔥 1.2M
  2. 标题2  🔥 850K
  ...
📅 2026-01-08 12:04:56  30条  ▶
```

---

## ⚠️ 重要注意事项

### 1. GitHub API 限制

| 认证状态 | 每小时限制 | 影响 |
|----------|------------|------|
| 未认证 | 60次 | 前端频繁刷新会受限 |
| 认证 | 5000次 | 完全够用 |

**解决方案**:
- ✅ 前端已添加 5分钟缓存
- ✅ 限制手动刷新频率
- ✅ 建议：添加 GitHub Token (可选)

### 2. 数据文件大小

| 项目 | 数值 |
|------|------|
| 单条快照 | ~50KB |
| 50条快照 | ~2.5MB |
| 仓库大小限制 | 100MB (推荐 < 50MB) |

**解决方案**:
- ✅ 自动保留最近50条
- ✅ 如需更多，修改 `MAX_SNAPSHOTS`
- ✅ 定期手动清理旧数据

### 3. 提交频率

| 频率 | 每日提交数 | 影响 |
|------|-----------|------|
| 每30分钟 | 48次/天 | Git 历史较频繁 |

**解决方案**:
- ✅ 可修改为每1小时 (cron: '0 */60 * * * *')
- ✅ 使用单独的 data 分支
- ✅ 或合并多次提交

### 4. 私有仓库

- GitHub Pages 对私有仓库需要 **GitHub Pro** 账号
- **解决方案**: 使用 **Public** 仓库 (推荐)

---

## 🔧 故障排查

### 问题 1: Actions 执行失败

```bash
# 检查 Actions 日志
# 访问: GitHub → Actions → 自动抓取知乎热榜 → 最近运行

# 常见原因:
# 1. 网络问题 (知乎 API 不可用)
# 2. 脚本错误 (检查 scripts/fetch.js)
# 3. 权限问题 (检查 Settings → Actions → General → Read and write permissions)
```

### 问题 2: 前端无法加载数据

```javascript
// 浏览器控制台检查
// 1. 检查配置是否正确
console.log(config.value)

// 2. 手动测试 API
fetch('https://api.github.com/repos/your-username/first_page/contents/data/zhihu-hot.json')
  .then(r => r.json())
  .then(d => console.log(atob(d.content)))
```

### 问题 3: 数据文件不存在

**解决方案**:
1. 手动触发 Actions 一次
2. 检查 Actions 是否成功执行
3. 查看仓库中是否有 `data/zhihu-hot.json`

### 问题 4: API 限制 (403 错误)

**解决方案**:
1. 等待1小时后重试
2. 或添加 GitHub Token:
   - Settings → Developer settings → Personal access tokens
   - 在 fetch.js 中添加: `headers: { 'Authorization': 'token YOUR_TOKEN' }`

---

## 📊 性能对比

| 指标 | 纯 GitHub 方案 | 传统云服务器 |
|------|----------------|--------------|
| **月成本** | ¥0 | ¥50-100 |
| **维护成本** | 0 | 需要维护 |
| **部署难度** | ⭐⭐ | ⭐⭐⭐⭐ |
| **稳定性** | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **响应速度** | ⭐⭐⭐ (有缓存) | ⭐⭐⭐⭐⭐ |
| **数据持久性** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

---

## 🎯 推荐配置

### 最小配置 (免费)
- ✅ Public 仓库
- ✅ GitHub Pages 启用
- ✅ Actions 权限开启
- ✅ 前端页面访问正常

### 完整配置
- ✅ 自定义域名
- ✅ HTTPS 证书
- ✅ 数据缓存优化
- ✅ 错误处理完善
- ✅ 自动清理旧数据

---

## 📝 总结

**纯 GitHub 方案完全可行！**

### 优势
- 💰 **完全免费** - 0元/月
- 🤖 **全自动化** - 定时抓取，无需人工干预
- 📊 **数据持久化** - 存储在 GitHub 仓库
- 🚀 **部署简单** - 提交代码即可运行
- 🔄 **灵活控制** - 支持手动触发和自动执行

### 只需要
1. ✅ 创建抓取脚本 (`scripts/fetch.js`)
2. ✅ 配置 GitHub Actions (`.github/workflows/fetch-hot.yml`)
3. ✅ 更新前端组件 (`src/components/ZhihuHot.vue`)
4. ✅ 提交代码并手动触发一次

**无需任何服务器费用！** 🎉

---

## 📚 相关文档

- `GITHUB_ONLY_ARCHITECTURE.md` - 架构说明
- `DEPLOYMENT_CHECKLIST.md` - 部署检查清单
- `scripts/fetch.js` - 抓取脚本源码
- `.github/workflows/fetch-hot.yml` - Actions 配置

---

*版本: 3.0.0*
*类型: 纯 GitHub 解决方案*
*成本: ¥0/月*
*最后更新: 2026-01-08*