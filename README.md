# first_page 🚀

这是一个使用 Vue 3 + Vite 构建的现代化网站项目，部署在 GitHub Pages 上。

## 📁 项目结构

```
first_page/
├── src/
│   ├── main.js           # Vue 入口文件
│   ├── App.vue           # 主组件
│   └── components/       # Vue 组件目录
├── public/               # 静态资源
├── .github/
│   └── workflows/
│       └── deploy.yml    # GitHub Actions 自动部署
├── index.html            # HTML 入口
├── package.json          # 项目依赖
├── vite.config.js        # Vite 配置
├── CNAME                 # 自定义域名配置（可选）
└── README.md             # 说明文档
```

## 🌐 部署方法

### 方法 1：使用 GitHub Actions 自动部署（推荐）

1. **安装依赖并本地测试**
   ```bash
   npm install
   npm run dev  # 本地开发测试
   npm run build  # 构建生产版本
   ```

2. **推送代码到 GitHub**
   ```bash
   git add .
   git commit -m "Add Vue 3 + Vite project with GitHub Actions"
   git push origin main
   ```

3. **启用 GitHub Pages**
   - 访问你的 GitHub 仓库
   - 点击 **Settings** → **Pages**
   - 在 **Build and deployment** 部分选择：
     - **Source**: `GitHub Actions`
   - GitHub 会自动检测 `.github/workflows/deploy.yml` 并开始构建

4. **访问你的网站**
   - 等待 1-2 分钟，Actions 会自动构建并部署
   - 访问：`https://<你的用户名>.github.io/first_page/`

### 方法 2：手动使用 gh-pages 部署

1. **安装依赖**
   ```bash
   npm install
   ```

2. **构建并部署**
   ```bash
   npm run deploy
   ```

3. **启用 GitHub Pages**
   - 在 **Settings** → **Pages** 中选择 `gh-pages` 分支
   - 访问网站

### 方法 2：使用自定义域名

1. 在 `CNAME` 文件中填入你的域名（例如：`example.com`）
2. 在域名提供商处添加 DNS 记录：
   - A 记录指向：`185.199.108.153`、`185.199.109.153`、`185.199.110.153`、`185.199.111.153`
   - 或者 CNAME 记录指向：`<你的用户名>.github.io`
3. 在 GitHub Pages 设置中配置自定义域名

## 🎨 开发指南

### 本地开发
```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览构建结果
npm run preview
```

### 自定义页面

**修改 Vue 组件** (`src/App.vue`)：
- 编辑 `<template>` 部分修改页面结构
- 修改 `<script setup>` 中的逻辑
- 调整 `<style>` 中的样式

**添加新组件**：
```bash
# 在 src/components/ 目录下创建新组件
touch src/components/MyComponent.vue
```

**修改主题颜色**：
在 `src/App.vue` 的 `<style>` 部分修改 CSS 变量：
```css
/* 修改这些颜色 */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

## 🚀 部署选项对比

| 方法 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| **GitHub Actions** | 自动化、无需手动操作 | 需要配置 | 推荐，适合持续开发 |
| **gh-pages CLI** | 简单快速 | 需要手动执行 | 快速部署，简单项目 |
| **静态 HTML** | 无需构建 | 功能有限 | 纯静态内容 |

## 📝 注意事项

- GitHub Pages 仅支持静态文件（构建后的文件）
- 每次推送后，Actions 部署需要 1-3 分钟
- 确保 `vite.config.js` 中的 `base` 路径正确设置
- 生产环境会自动压缩和优化资源
- 支持 Vue 单文件组件 (SFC)
- 可以使用 Vue 3 的所有特性

## 🔗 有用链接

- [Vue 3 官方文档](https://vuejs.org/)
- [Vite 官方文档](https://vitejs.dev/)
- [GitHub Pages 官方文档](https://docs.github.com/pages)
- [GitHub Actions 文档](https://docs.github.com/actions)
- [GitHub Pages + Vite 部署指南](https://vitejs.dev/guide/static-deploy.html#github-pages)

## 💡 常见问题

**Q: 为什么页面显示空白？**
A: 检查浏览器控制台错误，确保所有依赖已安装

**Q: 如何添加路由？**
A: 可以使用 `vue-router`，在 `package.json` 中添加依赖，然后配置路由

**Q: 如何使用状态管理？**
A: 可以使用 `pinia`，这是 Vue 3 推荐的状态管理库

**Q: 如何部署到自定义域名？**
A: 在 `CNAME` 文件中添加域名，并在域名 DNS 中添加 GitHub Pages 的 A 记录

---

**祝您的 Vue 网站运行顺利！** 🎉